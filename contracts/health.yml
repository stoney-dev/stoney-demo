version: 1
feature: demo
description: Simple demo for Stoney (HTTP)

contracts:
  - name: health
    description: Health endpoint must always respond correctly.
    checks:
      - id: health_ok
        work_item: "KAN-123"
        says: "Health endpoint must respond with 200 and ok=true"
        steps:
          - http:
              method: GET
              path: /health
            expect:
              status: 200
              json:
                ok: true